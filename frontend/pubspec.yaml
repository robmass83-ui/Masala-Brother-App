name: brotherapp
description: Masala Brother App — gestione spese condivise Roberto e Laura
publish_to: 'none'
version: 1.0.2+5

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^15.1.2
  google_fonts: ^6.2.1
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  # Firebase (wired in Fase 2; options file is gitignored)
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.6
  firebase_storage: ^12.4.5
  google_sign_in: ^6.3.0
  # Models
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  # Notifications / media / export (used in later phases)
  flutter_local_notifications: ^19.2.1
  timezone: ^0.10.1
  excel: ^4.0.6
  pdf: ^3.11.3
  printing: ^5.14.2
  share_plus: ^11.0.0
  path_provider: ^2.1.5
  archive: ^3.6.1
  file_picker: ^10.1.9
  image_picker: ^1.1.2
  flutter_image_compress: ^2.4.0
  connectivity_plus: ^6.1.4
  collection: ^1.19.1
  uuid: ^4.5.1
  url_launcher: ^6.3.1
  http: ^1.4.0
  open_filex: ^4.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15
  freezed: ^3.0.6
  json_serializable: ^6.9.5

flutter:
  uses-material-design: true
